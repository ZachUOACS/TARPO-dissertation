# Dependencies for TARPO on the UoA ML cluster.
#
# NOTE: the repo-root ./transformers, ./trl and ./unsloth directories are
# MODIFIED copies (the ActionHead lives in ./transformers/models/qwen2) and they
# shadow the pip-installed ones whenever python is launched from the repo root.
# The pip versions below are installed anyway so that version metadata checks
# inside unsloth / unsloth_zoo see the same versions the vendored code expects:
#   vendored transformers == 4.50.3
#   vendored trl          == 0.15.2
#   vendored unsloth      == 2025.3.19
# ALWAYS run from the repo root, and check with cluster/check_env.py.
#
# torch + xformers are installed separately by setup_env.sh (CUDA wheel index).

transformers==4.50.3
trl==0.15.2
peft>=0.14,<0.16
accelerate>=1.4,<1.7
datasets>=3.4,<4.0
huggingface_hub>=0.30,<0.32
unsloth_zoo>=2025.3.17,<2025.4  # must match vendored unsloth 2025.3.19
bitsandbytes>=0.45,<0.46      # needed for the default optim=paged_adamw_8bit
# (unsloth_zoo pulls tyro / cut_cross_entropy / hf_transfer itself)
sentencepiece
protobuf
numpy<2.3
pandas
scipy
tqdm
psutil
packaging

# reward / evaluation
math-verify
latex2sympy2_extended
sympy

# logging
wandb
